{
  "mcpServers": {
    "stock-research": {
      "command": "python",
      "args": [
        "-m",
        "backend.mcp_server"
      ],
      "transport": "stdio",
      "env": {
        "NVIDIA_API_KEY": "${NVIDIA_API_KEY}",
        "TAVILY_API_KEY": "${TAVILY_API_KEY}",
        "PYTHONPATH": "/Users/tranthihieu/Documents/project/stock-research-agent"
      },
      "description": "Multi-agent stock research server using CrewAI and MCP protocol"
    }
  },
  "version": "1.0.0",
  "name": "stock-research-mcp",
  "description": "MCP server configuration for AI-powered stock research with 7 specialized agents"
}
